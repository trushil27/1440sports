# Origin of the FastAPI service (api/). Read at BUILD time: next.config.ts proxies
# /api/* and /auth/* to it so the session cookie stays first-party. Default :8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

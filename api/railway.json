{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/app/pipeline/entrypoint.sh uvicorn intel_api.app:get_app --factory --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  },
  "_notes": [
    "Set this file as the service's config path (Service → Settings → Config-as-code) for the API service.",
    "Same variables as the pipeline plus APP_SECRET_KEY, APP_USERS, APP_RP_ID, APP_ORIGIN, API_BASE_URL, APP_COOKIE_SECURE=true.",
    "The API reads PDFs from PDF_STORAGE_DIR — mount the same volume as the cron job."
  ]
}

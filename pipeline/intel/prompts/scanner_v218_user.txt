Run today's signal scan. Search broadly for fresh F1/Formula E sponsorship targets in the past 7-14 days using multiple distinct searches. Apply V2.1 scoring and the alumni database. Return the ten strongest qualifying signals as one ranked JSON array, best first. THREE must be FE-track. For every candidate, run the matching protocol: walk the full grid (all 11 F1 teams and all current FE teams per active_sponsor_db.md), match the company's archetype against documented open categories, pick the team whose gap most cleanly fits — never by archetype default. If your final ten cluster on one or two teams, you have defaulted; redistribute by re-running the matching protocol. Do not self-censor — return real scores for the real top ten. Deduplication is enforced downstream in code. No preamble, no markdown.
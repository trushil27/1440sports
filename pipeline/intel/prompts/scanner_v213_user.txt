Run today's signal scan. Search broadly for fresh F1/Formula E sponsorship targets in the past 7-14 days using multiple distinct searches. Apply V2.1 scoring (5 dims × /20 each, /100 cap) and the alumni database. Cross-check candidates against active_sponsor_db.md for the Gate 5 saturation penalty. Of the ten signals you return, AT LEAST THREE must be FE-track (recommended_series: 'FE') — this is mandatory, not a preference. If your default searches turn up zero FE candidates, run dedicated FE-archetype searches (EV charging, battery storage, carbon accounting SaaS, sovereign-wealth electrification, sustainable fuels) and lower the FE-only floor to 40+/100 to fill the quota. Return the ten strongest qualifying signals as one ranked JSON array, best first, with the FE quota satisfied. Do not self-censor — return real scores for the real top ten. Deduplication is enforced downstream in code. No preamble, no markdown.